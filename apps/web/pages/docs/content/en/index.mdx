---
title: Welcome to Our Documentation
description: Comprehensive guides and documentation to help you start working with our product
---

# Getting Started with TypeScript

## Introduction
TypeScript is a powerful superset of JavaScript that adds static typing. Let's explore some basic concepts.

### Code Example

```typescript
interface User {
  id: number;
  name: string;
  email?: string; // Optional property
}

// Function with type annotations
function greetUser(user: User): string {
  return `Hello, ${user.name}!`;
}

// Example usage
const newUser: User = {
  id: 1,
  name: "John Doe"
};

console.log(greetUser(newUser)); // Output: Hello, John Doe!
```

## Data Types Table

| Type      | Example           | Description                   |
|-----------|------------------|-------------------------------|
| string    | "Hello"         | Text data                     |
| number    | 42, 3.14        | Numeric values                |
| boolean   | true/false      | Logical values                |
| array     | [1, 2, 3]       | Ordered collection            |

## Important Notes

> 💡 TypeScript compiles into JavaScript, making it compatible with all JavaScript environments.

### Features List
- Type Inference
- Interface Declaration
- Optional Properties
- Generic Types

## Math Example
Here's a simple math equation:


$$ E = mc^2 $$

# Встроенные формулы
Это формула внутри текста: $E = mc^2$

# Блочные формулы
$$
\begin{equation}
\frac{n!}{k!(n-k)!} = \binom{n}{k}
\end{equation}
$$

# Сложные формулы
$$
\begin{align*}
  & \phi(x,y) = \phi \left(\sum_{i=1}^n x_ie_i, \sum_{j=1}^n y_je_j \right)
  = \sum_{i=1}^n \sum_{j=1}^n x_i y_j \phi(e_i, e_j) = \\
  & (x_1, \ldots, x_n) \left( \begin{array}{ccc}
      \phi(e_1,e_1) & \cdots & \phi(e_1,e_n) \\
      \vdots & \ddots & \vdots \\
      \phi(e_n,e_1) & \cdots & \phi(e_n,e_n)
    \end{array} \right)
  \left( \begin{array}{c}
      y_1 \\
      \vdots \\
      y_n
    \end{array} \right)
\end{align*}
$$

## Component Example
You can use React components directly in MDX:

```tsx
import { useState } from 'react'

export function Counter() {
  const [count, setCount] = useState(0)

  return (
    <button onClick={() => setCount(c => c + 1)}>
      Count: {count}
    </button>
  )
}
```

Here's how you would use it:

<Counter />

# Пример кода с подсветкой

```javascript {1,3} /console.log/
const x = 1;
const y = 2;
console.log(x + y);
```

В этом примере:
- Первая и третья строки будут подсвечены (через `{1,3}`)
- Слово `console.log` будет дополнительно выделено (через `/console.log/`)
- В углу будет показано "javascript"
- Будет применена тема github-dark
- Строки будут разделены сеткой

Вы можете настроить стили для классов `highlighted` и `word--highlighted` в вашем CSS чтобы определить как именно будут выглядеть подсвеченные элементы.

# Базовый пример
```js
console.log('hello!')
```

# С нумерацией строк и подсветкой
```js showLineNumbers {3} title="example.js"
const x = 1
const y = 2
console.log(x + y)
```

# С метаданными для подсветки диапазона строк
```js {1,3-4}
import lib from 'lib'
const a = 1
const b = 2
console.log(a + b)
```

# С подсветкой слов
```js /hello/
console.log('hello!')
```
